MATCH (a)-[r]->(b) WHERE euid(r)="0_3937_0_0_0" RETURN a,b
找到符合(a)-[r]->(b)关系，过滤条件是 euid(r)的值必须等于0_3937_0_0_0，返回所有符合条件的a,b节点。
MATCH (n) RETURN n
返回数据库中的所有节点。
MATCH (n) RETURN n
返回数据库中的所有匹配到的节点。
MATCH (n) RETURN n
在数据库中的所有节点中，跳过第一个节点，并返回接下来最多100个节点。
MATCH (n) RETURN n.name LIMIT 10
节点n,返回n.name,只保留前10项。
MATCH (a {name: 'Carrie-Anne Moss'}) RETURN a.born AS SomethingTotallyDifferent
查询图数据库中名称为Carrie-Anne Moss的节点，并返回该节点的 born 属性值，且将该值重命名为SomethingTotallyDifferent。
MATCH (a {name: 'Carrie-Anne Moss'}) RETURN a.born AS SomethingTotallyDifferent
查找名称为Carrie-Anne Moss的节点,返回该节点的born属性值，并将其重命名为SomethingTotallyDifferent。
MATCH (a)-[r]->(b) WHERE euid(r)="0_3937_0_0_0" RETURN a,b
找到符合(a)-[r]->(b)关系，过滤条件是 euid(r)的值必须等于0_3937_0_0_0，返回所有符合条件的a,b节点。
MATCH (a)-[r]->(b) WHERE euid(r)="0_3937_0_0_0" RETURN a,b
查找图数据库中具有euid=0_3937_0_0_0关系，并返回与这些关系相关联的两个节点。
MATCH (a)-[r]->(b) WHERE euid(r)="0_3937_0_0_0" RETURN a,b
找到数据库中具有euid=0_3937_0_0_0的关系，并返回其相连的节点对。
MATCH (n) WHERE n.belt = 'white' OR n.belt IS NULL RETURN n.name, n.age, n.belt ORDER BY n.name
从图数据库中查找属性belt为white或为空的节点n，返回这些节点n的name、age和belt属性，同时按照节点的name属性排序。
MATCH (n) WHERE n.belt = 'white' OR n.belt IS NULL RETURN n.name, n.age, n.belt ORDER BY n.name
查找所有belt属性值为'white' 或belt属性为空的节点，返回这些节点的name、age和belt属性值，按照节点的name属性值升序排列返回的结果。
MATCH (n) WHERE n.belt = 'white' OR n.belt IS NULL RETURN n.name, n.age, n.belt ORDER BY n.name
查找具有belt属性值为'white'或belt属性为空的所有节点，并返回这些节点的name、age和belt属性值，同时按照name属性值升序排列返回的结果。
MATCH (a) WHERE a.name >= 'Peter' RETURN a.name, a.born
筛选出name属性值大于或等于'Peter'的节点a，返回节点a的name和born属性值。
MATCH (a) WHERE a.name >= 'Peter' RETURN a.name, a.born
找所有name属性值大于或等于'Peter'的节点，并返回这些节点的name 和 born属性值。
MATCH (a) WHERE a.name >= 'Peter' RETURN a.name, a.born
筛选出名称在字典序上等于或在'Peter'之后的所有节点，返回所有节点的name和born属性值。
MATCH (n:person) RETURN n.name ORDER BY n.name SKIP 1 LIMIT 2
从图数据库中查找所有带有person标签的节点，并按照节点的name属性值排序，跳过第一个结果，返回接下来的两个结果name属性值。
MATCH (n:person) RETURN n.name ORDER BY n.name SKIP 1 LIMIT 2
查找所有带有person标签的节点，按照name属性值进行升序排序，跳过排序后的第一个结果，返回接下来的两个结果的name属性值。
MATCH (n:person) RETURN n.name ORDER BY n.name SKIP 1 LIMIT 2
查询标签为person的节点的相关信息，结果按id降序排序，保留去除第1个数据后的2个数据。
WITH ['one','two','three'] AS coll RETURN coll, head(coll)
将'one','two','three'作为临时结果集coll，返回coll和coll的第一个元素。
WITH ['one','two','three'] AS coll RETURN coll, last(coll)
将'one','two','three'作为临时结果集coll，返回coll和coll的最后一个元素。
RETURN toFloat('11.5')
将11.5转为浮点数并返回。
RETURN toInteger('2.3') AS integer
将2.3转为整数后返回，并重命名为integer。
WITH ['Anne', 'John', 'Bill', 'Diane', 'Eve'] AS names RETURN names[1..3] AS result
将'Anne', 'John', 'Bill', 'Diane', 'Eve'作为临时结果集names，从names中检索数据，并返回names列表中的第二到四个元素。
WITH ['Anne', 'John', 'Bill', 'Diane', 'Eve'] AS names RETURN names[1..3] AS result
将'Anne', 'John', 'Bill', 'Diane', 'Eve'作为临时结果集names，从names中检索数据，并返回其中的第二到四个元素。