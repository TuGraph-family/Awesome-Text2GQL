MATCH (a {name: 'Carrie-Anne Moss'}) RETURN a.born AS born
查找Carrie-Anne Moss的出生年份
MATCH p = (person {name: 'Michael Douglas'})-[]->() RETURN p
查询Michael Douglas的出边路径，返回这些路径
MATCH (n:person) RETURN n.name, n.age ORDER BY n.name
获得数据库中所有的人的姓名和年龄，按照姓名排序。
MATCH (p:plan {name: "面壁计划"})-[e]-(neighbor:person) RETURN p,e,neighbor
与面壁计划有关的人有哪些？返回相应的边和节点。
MATCH (n)-[e:person_person]-(m) RETURN n,e,m
查找图中通过关系person_person相连的节点，返回节点和关系
MATCH (m:movie {title: 'Forrest Gump'})<-[:acted_in]-(a:person) RETURN a, m
查询电影Forrest Gump中的演员有哪些？返回电影和演员。
MATCH (u:user {login: 'Michael'})-[r:rate]->(m:movie) RETURN m.title, r.stars
查找登录用户Michael打分的电影，返回电影的名称和打分数。